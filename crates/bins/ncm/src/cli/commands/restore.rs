pub async fn handle(
    service: Option<Vec<String>>,
    node: Option<Vec<String>>,
    all: bool,
    target: Option<String>,
    force: bool,
    no_input: bool,
    no_tui: bool,
) -> anyhow::Result<()> {
    todo!(
        "Restore (service: {:?}, node: {:?}, all: {}, target: {:?}, force: {}, no_input: {}, no_tui: {})",
        service,
        node,
        all,
        target,
        force,
        no_input,
        no_tui
    )
}
