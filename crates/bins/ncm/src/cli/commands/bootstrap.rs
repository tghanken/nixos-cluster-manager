pub async fn handle(machines: Vec<String>, no_input: bool, no_tui: bool) -> anyhow::Result<()> {
    todo!(
        "Bootstrap machines {:?} (no_input: {}, no_tui: {})",
        machines,
        no_input,
        no_tui
    )
}
