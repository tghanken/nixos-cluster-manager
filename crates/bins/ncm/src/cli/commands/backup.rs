pub async fn handle(
    service: Option<Vec<String>>,
    node: Option<Vec<String>>,
    all: bool,
    target: Option<String>,
    no_input: bool,
    no_tui: bool,
) -> anyhow::Result<()> {
    todo!(
        "Backup (service: {:?}, node: {:?}, all: {}, target: {:?}, no_input: {}, no_tui: {})",
        service,
        node,
        all,
        target,
        no_input,
        no_tui
    )
}
